import {type Language, findLanguage, languages} from '@utils/languages.tsx';
import {type Theme, findTheme, themes} from '@utils/themes.ts';

export type { Language, Theme };
export { languages, themes, findLanguage, findTheme };
